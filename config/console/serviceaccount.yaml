# The OAuth client is a dedicated OAuthClient resource (created at install
# time, see README), not this service account: a service-account OAuth client
# may not request the user:full scope that token forwarding needs.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: agent-console
