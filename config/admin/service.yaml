apiVersion: v1
kind: Service
metadata:
  name: openclaw-admin-dashboard
  labels:
    app.kubernetes.io/name: openclaw-admin-dashboard
spec:
  selector:
    app.kubernetes.io/name: openclaw-admin-dashboard
  ports:
    - name: oauth
      port: 4180
      targetPort: oauth
